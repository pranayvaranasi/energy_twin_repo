version: '3.8'

services:
  digital-twin:
    build: .
    container_name: energy_supply_twin
    ports:
      - "8501:8501"
    environment:
      - PYTHONUNBUFFERED=1
      - STREAMLIT_SERVER_HEADLESS=true
    # volumes:
    #   - .:/app  # Commented out for submission to avoid overwriting the compiled Linux binary
